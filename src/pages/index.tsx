import { Text } from '@chakra-ui/react';
import { Container } from '@components';
import { Fragment } from 'react';

const Home = () => {
  return (
    <Container>
      <Text>Teast</Text>
    </Container>
  );
};

export default Home;
